and(
    ==(
        vehicle-ego.arrive_time,
        vehicle.arrive_time
    ),
    >(
        vehicle-ego.arrive_time,
        0
    ),
    is_direction(
        vehicle-ego.lane,
        left
    ),
    or(
        and(
            is_int_val(
                vehicle.lane.way_id,
                2
            ),
            or(
                is_direction(
                    vehicle.lane,
                    right
                ),
                is_direction(
                    vehicle.lane,
                    straight
                )
            )
        ),
        and(
            or(
                is_int_val(
                    vehicle.lane.way_id,
                    1
                ),
                is_int_val(
                    vehicle.lane.way_id,
                    3
                )
            ),
            is_direction(
                vehicle.lane,
                straight
            )
        )   
    )
)