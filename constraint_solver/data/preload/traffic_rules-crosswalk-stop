and(
  or(
    and(
      <=(
        +(
          apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s,
          *(
            -(
              1.0
            ),
            struct.apollo::hdmap::PathOverlap.end_s
          )
        ),
        apollo::planning::TrafficRule.config_.crosswalk.min_pass_s_distance
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      or(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        )
      ),
      not(
        apollo::planning::ReferenceLine.IsOnLane
      ),
      <=(
        +(
          struct.apollo::hdmap::PathOverlap.start_s132,
          *(
            -(
              1.0
            ),
            apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
          )
        ),
        40.0
      )
    ),
    and(
      <=(
        +(
          apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s,
          *(
            -(
              1.0
            ),
            struct.apollo::hdmap::PathOverlap.end_s
          )
        ),
        apollo::planning::TrafficRule.config_.crosswalk.min_pass_s_distance
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      or(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        )
      ),
      apollo::planning::ReferenceLine.IsOnLane
    ),
    and(
      <=(
        +(
          apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s,
          *(
            -(
              1.0
            ),
            struct.apollo::hdmap::PathOverlap.end_s
          )
        ),
        apollo::planning::TrafficRule.config_.crosswalk.min_pass_s_distance
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      not(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          )
        )
      ),
      or(
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          not(
            >=(
              GetADCStopDeceleration,
              apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
            )
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          >=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          apollo::planning::ReferenceLine.IsOnRoad,
          not(
            <=(
              apollo::common::SLPoint.s,
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.start_s
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          ),
          not(
            apollo::planning::ReferenceLine.IsOnRoad
          ),
          apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty,
          not(
            <=(
              apollo::common::math::Vec2d.InnerProd,
              0.0
            )
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        ),
        and(
          function(
            apollo::common::math::Polygon2d.IsPointIn,
            apollo::common::math::Polygon2d,
            apollo::common::math::Vec2d
          ),
          not(
            >=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_loose_l_distance
            )
          ),
          not(
            <=(
              min,
              apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
            )
          ),
          not(
            apollo::planning::Obstacle.reference_line_st_boundary.IsEmpty
          ),
          >=(
            GetADCStopDeceleration,
            apollo::planning::TrafficRule.config_.crosswalk.max_stop_deceleration
          ),
          <=(
            min,
            apollo::planning::TrafficRule.config_.crosswalk.stop_strict_l_distance
          )
        )
      ),
      not(
        apollo::planning::ReferenceLine.IsOnLane
      ),
      not(
        <=(
          +(
            struct.apollo::hdmap::PathOverlap.start_s132,
            *(
              -(
                1.0
              ),
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
            )
          ),
          40.0
        )
      )
    )
  ),
  apollo::planning::Crosswalk.FindCrosswalks
)