or(
  not(
    apollo::planning::scenario::Stage.config_.enabled
  ),
  and(
    apollo::planning::scenario::Stage.config_.enabled,
    apollo::planning::ReferenceLineInfo.GetOverlapOnReferenceLine,
    not(
      apollo::planning::scenario::Stage.FindTask
    )
  ),
  and(
    apollo::planning::scenario::Stage.config_.enabled,
    apollo::planning::ReferenceLineInfo.GetOverlapOnReferenceLine,
    apollo::planning::scenario::Stage.FindTask,
    ==(
      __dynamic_cast,
      0
    )
  ),
  and(
    or(
      and(
        apollo::planning::scenario::Stage.config_.enabled,
        apollo::planning::ReferenceLineInfo.GetOverlapOnReferenceLine,
        apollo::planning::scenario::Stage.FindTask,
        not(
          ==(
            __dynamic_cast,
            0
          )
        ),
        <=(
          +(
            struct.apollo::hdmap::PathOverlap.end_s,
            apollo::planning::CreepDecider.FindCreepDistance,
            *(
              -(
                1.0
              ),
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
            )
          ),
          0.0
        )
      ),
      and(
        apollo::planning::scenario::Stage.config_.enabled,
        apollo::planning::ReferenceLineInfo.GetOverlapOnReferenceLine,
        apollo::planning::scenario::Stage.FindTask,
        not(
          ==(
            __dynamic_cast,
            0
          )
        ),
        not(
          <=(
            +(
              struct.apollo::hdmap::PathOverlap.end_s,
              apollo::planning::CreepDecider.FindCreepDistance,
              *(
                -(
                  1.0
                ),
                apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
              )
            ),
            0.0
          )
        )
      )
    ),
    or(
      and(
        not(
          <=(
            apollo::perception::LightStatus.max_valid_stop_distance,
            +(
              struct.apollo::hdmap::PathOverlap.end_s60,
              apollo::planning::CreepDecider.7505.FindCreepDistance,
              *(
                -(
                  1.0
                ),
                apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
              )
            )
          )
        ),
        apollo::planning::Obstacle.IsVirtual
      ),
      and(
        <=(
          apollo::perception::LightStatus.max_valid_stop_distance,
          +(
            struct.apollo::hdmap::PathOverlap.end_s60,
            apollo::planning::CreepDecider.7505.FindCreepDistance,
            *(
              -(
                1.0
              ),
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
            )
          )
        ),
        >=(
          +(
            NowInSeconds,
            *(
              -(
                1.0
              ),
              apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.GetContext.creep_start_time
            )
          ),
          apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.scenario_config_.creep_timeout_sec
        ),
        apollo::planning::Obstacle.IsVirtual
      ),
      and(
        not(
          <=(
            apollo::perception::LightStatus.max_valid_stop_distance,
            +(
              struct.apollo::hdmap::PathOverlap.end_s60,
              apollo::planning::CreepDecider.7505.FindCreepDistance,
              *(
                -(
                  1.0
                ),
                apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
              )
            )
          )
        ),
        not(
          apollo::planning::Obstacle.IsVirtual
        ),
        apollo::planning::Obstacle.IsStatic
      ),
      and(
        <=(
          apollo::perception::LightStatus.max_valid_stop_distance,
          +(
            struct.apollo::hdmap::PathOverlap.end_s60,
            apollo::planning::CreepDecider.7505.FindCreepDistance,
            *(
              -(
                1.0
              ),
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
            )
          )
        ),
        >=(
          +(
            NowInSeconds,
            *(
              -(
                1.0
              ),
              apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.GetContext.creep_start_time
            )
          ),
          apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.scenario_config_.creep_timeout_sec
        ),
        not(
          apollo::planning::Obstacle.IsVirtual
        ),
        apollo::planning::Obstacle.IsStatic
      ),
      and(
        not(
          <=(
            apollo::perception::LightStatus.max_valid_stop_distance,
            +(
              struct.apollo::hdmap::PathOverlap.end_s60,
              apollo::planning::CreepDecider.7505.FindCreepDistance,
              *(
                -(
                  1.0
                ),
                apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
              )
            )
          )
        ),
        not(
          apollo::planning::Obstacle.IsVirtual
        ),
        not(
          apollo::planning::Obstacle.IsStatic
        ),
        <=(
          apollo::perception::LightStatus.min_boundary_t,
          apollo::planning::Obstacle.reference_line_st_boundary.min_t
        )
      ),
      and(
        <=(
          apollo::perception::LightStatus.max_valid_stop_distance,
          +(
            struct.apollo::hdmap::PathOverlap.end_s60,
            apollo::planning::CreepDecider.7505.FindCreepDistance,
            *(
              -(
                1.0
              ),
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
            )
          )
        ),
        >=(
          +(
            NowInSeconds,
            *(
              -(
                1.0
              ),
              apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.GetContext.creep_start_time
            )
          ),
          apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.scenario_config_.creep_timeout_sec
        ),
        not(
          apollo::planning::Obstacle.IsVirtual
        ),
        not(
          apollo::planning::Obstacle.IsStatic
        ),
        <=(
          apollo::perception::LightStatus.min_boundary_t,
          apollo::planning::Obstacle.reference_line_st_boundary.min_t
        )
      ),
      and(
        not(
          <=(
            apollo::perception::LightStatus.max_valid_stop_distance,
            +(
              struct.apollo::hdmap::PathOverlap.end_s60,
              apollo::planning::CreepDecider.7505.FindCreepDistance,
              *(
                -(
                  1.0
                ),
                apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
              )
            )
          )
        ),
        not(
          apollo::planning::Obstacle.IsVirtual
        ),
        not(
          apollo::planning::Obstacle.IsStatic
        ),
        not(
          <=(
            apollo::perception::LightStatus.min_boundary_t,
            apollo::planning::Obstacle.reference_line_st_boundary.min_t
          )
        ),
        not(
          <=(
            0.0,
            +(
              apollo::planning::STPoint.s,
              *(
                -(
                  1.0
                ),
                apollo::planning::STPoint.s
              )
            )
          )
        ),
        not(
          <=(
            apollo::perception::LightStatus.ignore_max_st_min_t,
            apollo::planning::Obstacle.reference_line_st_boundary.min_t
          )
        ),
        not(
          <=(
            apollo::planning::Obstacle.reference_line_st_boundary.min_s,
            apollo::perception::LightStatus.ignore_min_st_min_s
          )
        )
      ),
      and(
        <=(
          apollo::perception::LightStatus.max_valid_stop_distance,
          +(
            struct.apollo::hdmap::PathOverlap.end_s60,
            apollo::planning::CreepDecider.7505.FindCreepDistance,
            *(
              -(
                1.0
              ),
              apollo::planning::ReferenceLineInfo.AdcSlBoundary.end_s
            )
          )
        ),
        >=(
          +(
            NowInSeconds,
            *(
              -(
                1.0
              ),
              apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.GetContext.creep_start_time
            )
          ),
          apollo::planning::scenario::traffic_light::TrafficLightUnprotectedLeftTurnStageApproach.scenario_config_.creep_timeout_sec
        ),
        not(
          apollo::planning::Obstacle.IsVirtual
        ),
        not(
          apollo::planning::Obstacle.IsStatic
        ),
        not(
          <=(
            apollo::perception::LightStatus.min_boundary_t,
            apollo::planning::Obstacle.reference_line_st_boundary.min_t
          )
        ),
        not(
          <=(
            0.0,
            +(
              apollo::planning::STPoint.s,
              *(
                -(
                  1.0
                ),
                apollo::planning::STPoint.s
              )
            )
          )
        ),
        not(
          <=(
            apollo::perception::LightStatus.ignore_max_st_min_t,
            apollo::planning::Obstacle.reference_line_st_boundary.min_t
          )
        ),
        not(
          <=(
            apollo::planning::Obstacle.reference_line_st_boundary.min_s,
            apollo::perception::LightStatus.ignore_min_st_min_s
          )
        )
      )
    )
  )
)